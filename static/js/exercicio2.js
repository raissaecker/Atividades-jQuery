$(document).ready(function() {
    // Captura o evento de clique com o botão direito
    $(document).on("contextmenu", function(e) {
        e.preventDefault(); // Impede o comportamento padrão do menu de contexto
    });
});